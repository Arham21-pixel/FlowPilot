// App.jsx — Minimal setup
// The P5 team can add BrowserRouter, routes, layout etc. here
// For now, this just renders the Flow Builder (P1)
import React from 'react';
import BuilderPage from './pages/BuilderPage';

export default function App() {
  return <BuilderPage />;
}
